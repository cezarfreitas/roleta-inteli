# Configurações do Banco de Dados
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your-database-password
DB_NAME=sistema_fila

# Configurações de Autenticação JWT
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# Configurações de Webhook
WEBHOOK_URL=https://webhook.site/your-unique-url
WEBHOOK_TIMEOUT=5000

# Configurações da API de Leads (SprintHub)
LEAD_API_ID=grupointeli
LEAD_API_TOKEN=your-lead-api-token

# Configurações do Next.js
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Configurações do Docker (para desenvolvimento local)
# Descomente as linhas abaixo se estiver usando docker-compose
# DB_HOST=mysql
# DB_USER=root
# DB_PASSWORD=rootpassword
